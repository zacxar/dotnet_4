@page
@model SavedInSessionModel
@{
    ViewData["Title"] = "Data in session";
}
<p>Dane w zapisane w sesji</p>
@if (Model.PersonList != null)
{
    @*@foreach (var person in @Model.PersonList.list)
    {
        <p>
            @person
        </p>
    }*@
    @for (int i = 0; i < Model.PersonList.Length; i++)
    {
        <p>
            @Model.PersonList[i]
        </p>
    }
}

@*@foreach (var person in @Model.PersonList.list)
{
    <p>
        @person.Name, @person.Year - @person.Result
    </p>
}*@